SELECT
	CATEGORY,
	SUM(SALES) AS TOTAL_SALES
FROM
	BOOK b
INNER JOIN
	BOOK_SALES bs
ON 1 = 1
AND b.BOOK_ID = bs.BOOK_ID
WHERE 1 = 1
AND SUBSTR(bs.SALES_DATE, 1, 7) = '2022-01'
GROUP BY
	CATEGORY
ORDER BY
	CATEGORY ASC
;